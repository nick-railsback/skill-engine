# CHANGELOG

## [0.1.1] - 2026-05-18

- fix: the maintenance-agent's engine-doctor now validates `.schema_version == 1` instead of the retired `.version == "1.0"` field, eliminating activation-time failures on every fresh contextualizer (`plugin/skill-engine/engine-bootstrap-templates/maintenance-agent.md.template`)
- remove: two engine-doctor checks that validated retired schemas — one looked for a `research/sessions/` path the bootstrap never creates, the other validated an `importance` field from a superseded state-file shape. The orphan-sweep check is renumbered accordingly (`maintenance-agent.md.template`)
- fix: replace 27 references to `./test/test-cli.sh` (and the SHA-256 checksum fixture) with `./verify.sh` across worker-verify, maintenance-agent template, release-command template, and five doctrine docs. SHA-256 fixture references that describe coherent v0.2 design are preserved under an explicit "v0.2 aspirational" banner (`plugin/skill-engine/agents/worker-verify.md`, `plugin/skill-engine/engine-bootstrap-templates/{maintenance-agent,release-command}.md.template`, `plugin/skill-engine/docs/{01-principles,03-engine,05-invariants,06-release-doctrine,10-version-evolution}.md`)
- change: worker-refresh and worker-verify agents rewritten to be path-agnostic — they locate the contextualizer root at runtime instead of embedding `<area-domain>` template tokens, fixing the structural correctness bug where live plugin agents shipped with unsubstituted placeholders (`plugin/skill-engine/agents/worker-{refresh,verify}.md`)
- add: `.github/workflows/lint.yml` running shellcheck (`--severity=warning`) on all `*.sh` files and `jq empty` validation on production JSON files. First CI gate for the engine repo (`.github/workflows/lint.yml`)
- fix: align `CAPABILITIES.md`, `plugin/skill-engine/docs/02-artifact-contract.md`, `plugin/skill-engine/skills/new-reference/SKILL.md`, and three residual lines in `plugin/skill-engine/docs/03-engine.md` (REFRESH step 8, NEW step 5, SELF-AUDIT fix-flow Validate step) with the v0.1.x verify.sh-anchored validation model — remove stale "test suite" and "byte-equality fixture" present-tense claims, replace the 4-check pre-approval list with the canonical 3-check model (catalog bijection, no-frontmatter, `verify.sh`), and add a v0.1.x scope-note banner to `02-artifact-contract.md`
