# CHANGELOG

## [0.2.0] - 2026-05-19

- add: `web-doc` as a fourth first-class source kind alongside `git-managed`, `external-doc`, `local-path`. Documentation sites are crawled via the model's installed fetch tool (WebFetch / MCP fetch) with `crawl_mode: sitemap` or `crawl_mode: list`, bounded by `crawl_budget`, snapshotted to `~/.cache/skill-engine/web-doc/<source_id>-<crawl_id>/` with `source_url` / `crawl_date` / `decay` provenance frontmatter on every captured `.md` (`plugin/skill-engine/docs/{02-artifact-contract,03-engine,08-discover-pipeline,09-discover-config}.md`, `plugin/skill-engine/skills/discover/SKILL.md`, `plugin/skill-engine/docs/recipes/web-doc-setup.md`).
- add: `verify.sh` Checks 5.5 and 5.6 — external-doc + web-doc provenance frontmatter walk and `web-doc-snapshot-present` WARN — gated on per-source `kind` and `lifecycle.last_crawl_id`. Check 5.5 includes a realpath containment guard inside its walk loop so a symlink that escapes `CTX_ROOT` cannot be walked (`plugin/skill-engine/engine-bootstrap-templates/verify.sh`).
- change: cache layout is now kind-partitioned at `~/.cache/skill-engine/{git-managed,web-doc}/<source_id>-<sha-or-crawl_id>/`. REFRESH's pre-flight step 1.5 detects flat-layout legacy entries via an allow-list (`<entry>/.git/HEAD` shape, so future kinds are not misclassified) and offers a one-shot bulk `mv` into `git-managed/`, prompted once per session; decline → those entries re-clone on next REFRESH (`plugin/skill-engine/skills/refresh/SKILL.md`, `plugin/skill-engine/docs/03-engine.md`).
- add: `clean-cache` accepts an optional `[kind]` argument (`git-managed` or `web-doc`) that scopes the dry-run preview and deletion to one cache subtree (`plugin/skill-engine/skills/clean-cache/SKILL.md`).
- change: `engine-bootstrap` auto-detects source kind from URL shape and now proposes `web-doc` with `crawl_mode: sitemap` when an HTTP HEAD probe returns `Content-Type: text/html`; the `source-paths.json` enum template lists all four kinds (`plugin/skill-engine/skills/engine-bootstrap/SKILL.md`, `CAPABILITIES.md`).
- add: `.github/workflows/lint.yml` gains a `doctrine` job (grep-based enforcement: no html-to-markdown library bundled, no non-HEAD `curl` in engine shell, no `Authorization: Bearer` / `GITHUB_TOKEN` plumbing) and a `tests` job (three feature-scoped runners: web-doc 22 cases, clean-cache 3 cases, migration 6 cases) (`.github/workflows/lint.yml`, `plugin/skill-engine/tests/`).
- change: every doc, persona, and template that enumerated "three first-class source kinds" now reads "four" with `web-doc` included. Personas whose use-case was documentation-site ingestion (marketing pages, Confluence) now point at `web-doc`, not `external-doc` (`README.md`, `CAPABILITIES.md`, `plugin/skill-engine/docs/11-walkthrough.md`, `plugin/skill-engine/skills/new-reference/SKILL.md`, `plugin/skill-engine/engine-bootstrap-templates/navigator-multi-domain.md.template`, `docs/personas/{senior-product-manager,solutions-engineer}.md`).
- fix: `verify.sh` executable bit restored — the template flipped 100755 → 100644 mid-branch, which would have broken `./research/verify.sh` invocation on every newly stamped contextualizer (`plugin/skill-engine/engine-bootstrap-templates/verify.sh`).
- change: `doctrine.sh` self-exclusion is now path-based (`grep -v -F "$PLUGIN_ROOT/tests/doctrine.sh"`) rather than content-regex, and each check carries inline doctrine clauses (the script no longer cites an internal-only spec path) (`plugin/skill-engine/tests/doctrine.sh`).
- add: `_crawl-manifest.json` schema assertion in `tests/web-doc/run.sh` — required keys + types are now pinned via `jq -e` against the expected-snapshot fixture, so manifest drift fails CI (`plugin/skill-engine/tests/web-doc/run.sh`).
- change: test-harness tmpdir cleanup converted from per-function `RETURN` traps to explicit end-of-function `rm -rf` plus a top-level `EXIT` sweep of `skill-engine-{test,frontmatter,smoke,escape}.*` — `RETURN` traps required `functrace` that wasn't set; the inline comment that claimed an EXIT backstop existed was inaccurate (`plugin/skill-engine/tests/web-doc/run.sh`).

## [0.1.1] - 2026-05-18

- fix: the maintenance-agent's engine-doctor now validates `.schema_version == 1` instead of the retired `.version == "1.0"` field, eliminating activation-time failures on every fresh contextualizer (`plugin/skill-engine/engine-bootstrap-templates/maintenance-agent.md.template`)
- remove: two engine-doctor checks that validated retired schemas — one looked for a `research/sessions/` path the bootstrap never creates, the other validated an `importance` field from a superseded state-file shape. The orphan-sweep check is renumbered accordingly (`maintenance-agent.md.template`)
- fix: replace 27 references to `./test/test-cli.sh` (and the SHA-256 checksum fixture) with `./verify.sh` across worker-verify, maintenance-agent template, release-command template, and five doctrine docs. SHA-256 fixture references that describe coherent v0.2 design are preserved under an explicit "v0.2 aspirational" banner (`plugin/skill-engine/agents/worker-verify.md`, `plugin/skill-engine/engine-bootstrap-templates/{maintenance-agent,release-command}.md.template`, `plugin/skill-engine/docs/{01-principles,03-engine,05-invariants,06-release-doctrine,10-version-evolution}.md`)
- change: worker-refresh and worker-verify agents rewritten to be path-agnostic — they locate the contextualizer root at runtime instead of embedding `<area-domain>` template tokens, fixing the structural correctness bug where live plugin agents shipped with unsubstituted placeholders (`plugin/skill-engine/agents/worker-{refresh,verify}.md`)
- add: `.github/workflows/lint.yml` running shellcheck (`--severity=warning`) on all `*.sh` files and `jq empty` validation on production JSON files. First CI gate for the engine repo (`.github/workflows/lint.yml`)
- fix: align `CAPABILITIES.md`, `plugin/skill-engine/docs/02-artifact-contract.md`, `plugin/skill-engine/skills/new-reference/SKILL.md`, and three residual lines in `plugin/skill-engine/docs/03-engine.md` (REFRESH step 8, NEW step 5, SELF-AUDIT fix-flow Validate step) with the v0.1.x verify.sh-anchored validation model — remove stale "test suite" and "byte-equality fixture" present-tense claims, replace the 4-check pre-approval list with the canonical 3-check model (catalog bijection, no-frontmatter, `verify.sh`), and add a v0.1.x scope-note banner to `02-artifact-contract.md`
